# Tutor

Две вещи:

1. **Claude Tutor** подключается автоматически через `.claude/settings.json`: `/learn`, `/quiz`, `/review`, `/resources`.
   Прогресс (SM-2) лежит в `learning/` и коммитится.
2. **Уведомления.** Облачная Routine каждый день открывает сессию на этом репозитории и присылает push:
   если есть что повторить, запускает квиз.
